# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/

# Not used here, but just in case a toolchain ever appears
node_modules/

# NOTE: app/data.json and docs/dictionary.md are generated by scripts/, but they
# are intentionally NOT ignored — the app needs data.json at runtime when hosted,
# and dictionary.md is a handy committed reference. Regenerate with:
#   python3 scripts/build_data.py
#   python3 scripts/build_dictionary.py
